"use client";

// Ensure Builder registry runs on the client exactly once per app load
import React from "react";
import "./builder-registry";

export default function BuilderRegistryClient() {
  return null;
}


